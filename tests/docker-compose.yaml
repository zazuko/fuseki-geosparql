version: "3"

services:
  fuseki:
    build:
      context: ../
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
    ports:
      - 3030:3030
    environment:
      - ADMIN_PASSWORD=secret
